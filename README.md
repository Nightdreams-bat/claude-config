# Claude Code configuration

Backup of `~/.claude` — settings, hooks, custom skills, agents, slash commands, and the memory store.

Excluded (see `.gitignore`): credentials, `settings.local.json`, `.claude.json`, session transcripts, caches, downloads, and other machine-local state.
